{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RewardClaim",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "amount": {
      "type": "integer",
      "minimum": 1
    },
    "specifier": {
      "type": "string",
      "description": "Unique identifier for this claim instance, relative to the challenge ID. Typically, for rewards that can be claimed once per user, this is a user identifier."
    }
  },
  "required": [
    "id",
    "amount",
    "specifier"
  ],
  "additionalProperties": false
}
