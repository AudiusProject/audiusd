package server

import (
	"github.com/AudiusProject/audiusd/pkg/core/common"
	"github.com/labstack/echo/v4"
)

func (s *Server) Initialize(e *echo.Echo, l *common.Logger) (*Server, error) {
	// set defaults

	return s, nil
}
